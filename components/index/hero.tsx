import DynamicText from './dynamic-text';

const Hero = () => (
  <div id="hero">
    <h1 className="font-bold sm:text-9xl text-6xl py-5">Richard So</h1>
    <DynamicText />
  </div>
);

export default Hero;
