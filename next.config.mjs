/**
 * @type {import('next').NextConfig}
 */
const nextConfig = {
  swcMinify: true,
  // images: {
  //   loader: 'default',
  //   path: '',
  // },
  assetPrefix: './',
};

export default nextConfig;
